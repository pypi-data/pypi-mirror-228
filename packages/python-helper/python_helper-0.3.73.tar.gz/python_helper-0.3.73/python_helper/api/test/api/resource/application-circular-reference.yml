circular:
    reference:
        on:
            key: ${circular.reference.on.other-key}
            other-key: ${circular.reference.on.key}
    key: ${circular.other-key}
    other-key: ${circular.key}
