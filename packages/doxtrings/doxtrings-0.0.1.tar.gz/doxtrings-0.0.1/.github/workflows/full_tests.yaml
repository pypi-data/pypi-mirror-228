name: Test doXtrings library

on:
  pull_request:
    branches:
      - "main"

jobs:
  lint:
    uses: ./.github/workflows/lint.yaml

  test:
    strategy:
      fail-fast: false
      matrix:
        python_version: ["3.8", "3.9", "3.10", "3.11"]
    uses: ./.github/workflows/test.yaml
    with:
      python_version: ${{ matrix.python_version }}
