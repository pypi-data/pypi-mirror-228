[tool.poetry]
name = "typetidy"
version = "0.1.0"
description = "A linting tool to move typing imports into an optional block"
authors = ["Weevils.io <code@weevils.io>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.0"
pre-commit = "^3.3.3"
prospector = "^1.10.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
