from setuptools import setup, find_packages

setup(
    name='glr_utilities',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List any dependencies your utility functions might have
    ],
)