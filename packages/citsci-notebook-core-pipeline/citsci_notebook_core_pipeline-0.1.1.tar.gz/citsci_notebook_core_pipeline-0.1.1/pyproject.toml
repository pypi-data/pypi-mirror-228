[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "citsci_notebook_core_pipeline"
version = "0.1.1"
readme = "README.md"
dependencies = [
    "panoptes_client",
    'google.cloud.storage',
]