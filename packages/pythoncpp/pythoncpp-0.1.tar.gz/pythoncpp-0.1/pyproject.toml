[tool.poetry]
name = "pythoncpp"
version = "0.1"
description = "Python, compiled."
authors = ["dcode <opendylan@proton.me>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.scripts]
pycpp = 'pycpp.compile:main'

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
